# Generic, agent-agnostic baseline rules. Layered with each agent's
# default_rules.yaml (in agent_uplink/agents/<name>/) and the per-mode auth
# rule contributed by the agent.

rules:
  - name: default-readonly
    hosts: [".*"]
    methods: [GET, OPTIONS, HEAD]
